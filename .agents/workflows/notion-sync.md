---
description: One-way Notion sync of pipeline view (read-only).
---

When the user runs /notion-sync, follow `workflows/notion-sync.md`.

## Platform: Antigravity
- Shell: run_shell_command
- Reviewer: DefineSubagent / parallel subagent (pass drafts inline)
- Verify skills visible via /skills in CLI


User input: $ARGUMENTS
